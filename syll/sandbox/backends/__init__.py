"""Sandbox backends: concrete :class:`Environment` implementations.

- :class:`syll.sandbox.backends.docker.DockerEnvironment` — Docker desktop
  container with Docker-native snapshot/reset (commit + recreate).
"""
